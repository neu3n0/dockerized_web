version: '3'

services:
  backend-django:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend:/usr/src/app
    ports:
      - 8000:8000
    env_file:
      - "./.env.dev"